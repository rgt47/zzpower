% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_test_registry.R
\name{.compute_power}
\alias{.compute_power}
\title{Compose a power-function call for a single (test, design) pair}
\usage{
.compute_power(
  test_spec,
  sample_sizes,
  effect_size_std,
  alpha = 0.05,
  alternative = "two.sided"
)
}
\arguments{
\item{test_spec}{A registry entry.}

\item{sample_sizes}{Output of the spec's `sample_size_calc()`.}

\item{effect_size_std}{Standardized effect size (Cohen's d/h/f
or analogous).}

\item{alpha}{Type I error rate.}

\item{alternative}{`"two.sided"` or `"one.sided"`.}
}
\value{
Numeric scalar power, or `NA_real_` if the call errors.
}
\description{
Mirrors the call assembly inside `create_generic_test_server`'s
`.power_at_n()` closure but takes plain values rather than
Shiny `input` references. Centralising the call shape here lets
the programmatic API (`power_calc`, `power_table`) and the
Shiny server share one engine.
}
\keyword{internal}
