# Cluster-Randomized Log-rank Specification

Two-group cluster-randomized trial with a survival outcome. Schoenfeld
formula on the per-arm effective event count, where both event_prob and
design effect (DE) inflate the variance.

## Usage

``` r
create_cluster_logrank_spec()
```
