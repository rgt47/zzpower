# Citation text for inclusion in downloaded reports

Returns a one-line citation string. Pulls from the package's
\`inst/CITATION\` when available; otherwise falls back to a hard-coded
string keyed off the installed version.

## Usage

``` r
.zzpower_citation_line()
```
