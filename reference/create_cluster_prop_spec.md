# Cluster-Randomized Two-Proportion Specification

Two-group cluster-randomized trial with a binary outcome. Cohen's h on
the per-arm effective sample size; design effect inflates variance via
DE = 1 + (m_bar - 1) x ICC.

## Usage

``` r
create_cluster_prop_spec()
```
